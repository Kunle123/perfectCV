[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    auth: Authentication related tests
    users: User management related tests
    resumes: Resume related tests
    jobs: Job description related tests
    optimizations: Resume optimization related tests
    integration: Integration tests 